{% comment %}
=============================================================================
SECTION SHOPIFY 2.0 - ANALYSEUR EAU - ALGORITHME SCIENTIFIQUE v5.2
=============================================================================
Interface avec algorithme scientifique équitable et transparence complète
Version 5.2 - Scoring équitable Life Water
=============================================================================
{% endcomment %}

<div id="water-analyzer-{{ section.id }}" class="water-analyzer-section">
  <div class="page-width">
    <div class="water-analyzer-container">
      
      <div class="water-analyzer-header">
        <h2 class="h1">{{ section.settings.title | default: "Analyseur Scientifique Équitable de Qualité de l'Eau" }}</h2>
        {% if section.settings.description != blank %}
          <div class="water-analyzer-description rte">
            {{ section.settings.description }}
          </div>
        {% endif %}
      </div>

      <div id="analysis-tool-container-{{ section.id }}" class="analysis-tool-container">
        
        <div class="analysis-tool-header">
          <h3>🔬 Outil d'Analyse Scientifique v5.2</h3>
          <p>Découvrez la qualité de votre eau potable grâce à notre <strong>algorithme scientifique équitable</strong> basé sur les normes OMS et UE.</p>
          <p><strong>Nouveauté v5.2 :</strong> Transparence totale sur TOUS les paramètres importants - les non testés reçoivent le bénéfice du doute à 50/100.</p>
          <p><small>💡 Si aucune donnée n'est disponible pour votre commune, nous rechercherons automatiquement dans les communes voisines.</small></p>
        </div>

        <div id="analysis-form-{{ section.id }}" class="analysis-form">
          
          <div class="field field--with-error">
            <div class="address-input-wrapper">
              <input
                class="field__input"
                type="text"
                id="analysis-address-{{ section.id }}"
                placeholder="Tapez votre adresse : 123 rue de la République, 75001 Paris"
                required
              >
              <label class="field__label" for="analysis-address-{{ section.id }}">Adresse à analyser *</label>
              <div id="address-suggestions-{{ section.id }}" class="address-suggestions"></div>
            </div>
            <small id="address-error-{{ section.id }}" class="form__message" hidden>
              <span class="form__message-icon">⚠️</span>
              Veuillez sélectionner une adresse dans la liste
            </small>
            <div id="address-status-{{ section.id }}" class="address-status"></div>
          </div>

          <div class="field">
            <fieldset class="form__fieldset">
              <legend class="form__legend">Options d'analyse</legend>
              <div class="form__checkbox-wrapper">
                <input
                  type="checkbox"
                  id="analysis-detailed-{{ section.id }}"
                  class="form__checkbox"
                  value="oui"
                >
                <label for="analysis-detailed-{{ section.id }}" class="form__checkbox-label">
                  <span class="form__checkbox-text">
                    Analyse approfondie (inclut les détails techniques et l'accordéon par catégorie)
                  </span>
                </label>
              </div>
            </fieldset>
          </div>

          <div class="form__submit">
            <button type="button" class="button button--primary" id="start-water-analysis-{{ section.id }}">
              <span>🧪 Analyser avec l'algorithme équitable</span>
              <div class="loading-overlay__spinner hidden">
                <svg aria-hidden="true" focusable="false" role="presentation" class="spinner" viewBox="0 0 66 66" xmlns="http://www.w3.org/2000/svg">
                  <circle class="path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
                </svg>
              </div>
            </button>
          </div>

        </div>

        <div id="analysis-loading-{{ section.id }}" class="analysis-loading" style="display: none;">
          <div class="loading-content">
            <div class="loading-overlay__spinner">
              <svg aria-hidden="true" focusable="false" role="presentation" class="spinner" viewBox="0 0 66 66" xmlns="http://www.w3.org/2000/svg">
                <circle class="path" fill="none" stroke-width="6" cx="33" cy="33" r="30"></circle>
              </svg>
            </div>
            <p id="loading-text-{{ section.id }}" class="loading-text">Recherche des données de qualité d'eau...</p>
            <div id="progress-steps-{{ section.id }}" class="progress-steps">
              <div class="step" id="step-geocoding-{{ section.id }}">
                <span class="step-icon">📍</span>
                <span class="step-text">Géocodage</span>
              </div>
              <div class="step" id="step-data-{{ section.id }}">
                <span class="step-icon">🔍</span>
                <span class="step-text">Données</span>
              </div>
              <div class="step" id="step-analysis-{{ section.id }}">
                <span class="step-icon">🧮</span>
                <span class="step-text">Analyse</span>
              </div>
              <div class="step" id="step-scoring-{{ section.id }}">
                <span class="step-icon">🎯</span>
                <span class="step-text">Scoring</span>
              </div>
            </div>
          </div>
        </div>

        <div id="analysis-results-{{ section.id }}" class="analysis-results" style="display: none;">
          <!-- Les résultats Life Water v5.2 seront affichés ici après l'analyse -->
        </div>

      </div>

    </div>
  </div>
</div>

<style>
  /* ===== STYLES PRINCIPAUX ===== */
  .water-analyzer-section {
    padding: 5rem 0;
  }
  
  .water-analyzer-container {
    max-width: 80rem;
    margin: 0 auto;
  }
  
  .water-analyzer-header {
    text-align: center;
    margin-bottom: 3rem;
  }
  
  .water-analyzer-description {
    margin-top: 1rem;
    font-size: 1.6rem;
    opacity: 0.8;
  }
  
  /* ===== CONTAINER PRINCIPAL ===== */
  .analysis-tool-container {
    background: rgb(var(--color-background));
    border: 0.1rem solid rgba(var(--color-foreground), 0.1);
    border-radius: 1rem;
    padding: 3rem;
    margin-bottom: 2rem;
    box-shadow: 0 0.4rem 1.2rem rgba(var(--color-shadow), 0.1);
  }

  .analysis-tool-header {
    text-align: center;
    margin-bottom: 3rem;
    padding: 2rem;
    background: linear-gradient(135deg, rgba(var(--color-button), 0.1), rgba(var(--color-button), 0.05));
    border-radius: 1rem;
    border: 0.1rem solid rgba(var(--color-button), 0.2);
  }

  .analysis-tool-header h3 {
    margin: 0 0 1rem 0;
    color: rgb(var(--color-button));
    font-size: 2.4rem;
  }

  /* ===== FORMULAIRE ADRESSE ===== */
  .address-input-wrapper {
    position: relative;
  }

  .address-suggestions {
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    background: rgb(var(--color-background));
    border: 0.1rem solid rgba(var(--color-foreground), 0.2);
    border-top: none;
    border-radius: 0 0 var(--inputs-radius) var(--inputs-radius);
    max-height: 20rem;
    overflow-y: auto;
    z-index: 1000;
    display: none;
    box-shadow: var(--shadow-horizontal-offset) var(--shadow-vertical-offset) var(--shadow-blur-radius) rgba(var(--color-shadow), var(--shadow-opacity));
  }

  .address-suggestion {
    padding: 1.2rem;
    cursor: pointer;
    border-bottom: 0.1rem solid rgba(var(--color-foreground), 0.1);
    transition: background-color var(--duration-short) ease;
    font-size: 1.4rem;
  }

  .address-suggestion:hover {
    background: rgba(var(--color-button), 0.1);
  }

  .address-status {
    margin-top: 1rem;
    font-size: 1.3rem;
    padding: 1rem;
    border-radius: var(--inputs-radius);
    display: none;
  }

  .address-status.success {
    background: rgba(var(--color-success), 0.1);
    color: rgb(var(--color-success));
    border: 0.1rem solid rgba(var(--color-success), 0.2);
    display: block;
  }

  .address-status.error {
    background: rgba(var(--color-error), 0.1);
    color: rgb(var(--color-error));
    border: 0.1rem solid rgba(var(--color-error), 0.2);
    display: block;
  }

  /* ===== FORMULAIRE ===== */
  .form__fieldset {
    border: none;
    padding: 0;
    margin: 0;
  }

  .form__legend {
    font-weight: 600;
    margin-bottom: 1.5rem;
    font-size: 1.6rem;
  }

  .form__checkbox-wrapper {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .form__checkbox {
    margin-top: 0.2rem;
    width: 1.8rem;
    height: 1.8rem;
    flex-shrink: 0;
  }

  .form__checkbox-label {
    cursor: pointer;
    font-size: 1.4rem;
    line-height: 1.4;
  }

  /* ===== LOADING AMÉLIORÉ v5.2 ===== */
  .analysis-loading {
    text-align: center;
    padding: 3rem;
  }

  .loading-content {
    max-width: 50rem;
    margin: 0 auto;
  }

  .loading-text {
    margin: 2rem 0;
    font-size: 1.6rem;
    color: rgb(var(--color-foreground));
    min-height: 2.4rem;
  }

  .progress-steps {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-top: 3rem;
  }

  .step {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.8rem;
    opacity: 0.3;
    transition: opacity var(--duration-default) ease;
    min-width: 8rem;
  }

  .step.active {
    opacity: 1;
    color: rgb(var(--color-button));
  }

  .step.completed {
    opacity: 1;
    color: rgb(var(--color-success));
  }

  .step.error {
    opacity: 1;
    color: rgb(var(--color-error));
  }

  .step-icon {
    font-size: 2.4rem;
  }

  .step-text {
    font-size: 1.3rem;
    font-weight: 500;
    text-align: center;
  }

  .form__message {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: rgb(var(--color-error));
    font-size: 1.2rem;
    margin-top: 0.5rem;
  }

  /* ===== STYLES LIFE WATER v5.2 ===== */
  .life-water-report {
    border-radius: 15px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    background: white;
    margin-top: 2rem;
  }
  
  .life-water-header {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 30px;
    text-align: center;
  }
  
  .life-water-header h2 {
    margin: 0 0 20px 0;
    font-size: 2.5em;
    font-weight: 600;
  }
  
  .life-water-header p {
    margin: 10px 0;
    font-size: 1.1em;
    opacity: 0.95;
  }
  
  .life-water-header hr {
    border: none;
    height: 1px;
    background: rgba(255,255,255,0.3);
    margin: 20px 0;
  }
  
  .resultat-principal {
    background: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%);
    color: white;
    padding: 30px;
    text-align: center;
  }
  
  .resultat-principal h3 {
    margin: 0 0 20px 0;
    font-size: 2em;
  }
  
  .score-display {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 40px;
    margin: 30px 0;
    flex-wrap: wrap;
  }
  
  .score-circle {
    width: 150px;
    height: 150px;
    border: 8px solid;
    border-radius: 50%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background: white;
    font-weight: bold;
  }
  
  .score-number {
    font-size: 3em;
    line-height: 1;
  }
  
  .score-label {
    font-size: 1em;
    opacity: 0.7;
  }
  
  .score-info h4 {
    font-size: 2.5em;
    margin: 0 0 10px 0;
  }
  
  .score-message {
    font-size: 1.3em;
    margin: 0;
  }
  
  .content-section {
    padding: 30px;
  }
  
  .points-attention {
    background: #fff3cd;
    border-left: 5px solid #ffc107;
    padding: 20px;
    margin: 20px 0;
    border-radius: 0 10px 10px 0;
  }
  
  .recommandations {
    background: #d1ecf1;
    border-left: 5px solid #17a2b8;
    padding: 20px;
    margin: 20px 0;
    border-radius: 0 10px 10px 0;
  }
  
  .contributions-section {
    background: #f8f9fa;
    border-left: 5px solid #6c757d;
    padding: 20px;
    margin: 20px 0;
    border-radius: 0 10px 10px 0;
  }
  
  .details-section {
    background: #e8f5e8;
    border-left: 5px solid #28a745;
    padding: 20px;
    margin: 20px 0;
    border-radius: 0 10px 10px 0;
  }
  
  .footer-life-water {
    background: #e9ecef;
    padding: 30px;
    text-align: center;
    border-top: 1px solid #dee2e6;
  }
  
  .life-water-report ul {
    padding-left: 20px;
  }
  
  .life-water-report li {
    margin: 8px 0;
  }

  /* ===== RESPONSIVE ===== */
  @media screen and (max-width: 749px) {
    .water-analyzer-section {
      padding: 3rem 0;
    }
    
    .analysis-tool-container {
      padding: 2rem;
    }

    .progress-steps {
      gap: 1rem;
      flex-wrap: wrap;
    }

    .step {
      min-width: 6rem;
    }

    .score-display {
      flex-direction: column;
      gap: 20px;
    }

    .score-circle {
      width: 120px;
      height: 120px;
    }

    .score-number {
      font-size: 2.5em;
    }

    .content-section {
      padding: 20px;
    }
  }
</style>

<!-- Chargement des fichiers JavaScript v5.2 -->
<script src="{{ 'baremes-eau.js' | asset_url }}" defer></script>
<script src="{{ 'scoring-eau.js' | asset_url }}" defer></script>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const sectionId = '{{ section.id }}';
  
  const analysisForm = document.getElementById(`analysis-form-${sectionId}`);
  const analysisLoading = document.getElementById(`analysis-loading-${sectionId}`);
  const analysisResults = document.getElementById(`analysis-results-${sectionId}`);
  
  const addressInput = document.getElementById(`analysis-address-${sectionId}`);
  const suggestionsContainer = document.getElementById(`address-suggestions-${sectionId}`);
  const statusContainer = document.getElementById(`address-status-${sectionId}`);
  const startWaterAnalysisBtn = document.getElementById(`start-water-analysis-${sectionId}`);
  const loadingText = document.getElementById(`loading-text-${sectionId}`);
  
  let selectedAddress = null;
  let debounceTimer = null;

  console.log('🚀 ANALYSEUR EAU v5.2 - ALGORITHME SCIENTIFIQUE ÉQUITABLE INITIALISÉ');

  // ===== FONCTIONS UTILITAIRES ===== 
  
  function showAddressError(show = true) {
    const errorElement = document.getElementById(`address-error-${sectionId}`);
    if (errorElement) {
      errorElement.hidden = !show;
    }
    if (addressInput) {
      if (show) {
        addressInput.classList.add('field__input--error');
        addressInput.setAttribute('aria-invalid', 'true');
      } else {
        addressInput.classList.remove('field__input--error');
        addressInput.removeAttribute('aria-invalid');
      }
    }
  }

  function updateProgressStep(stepName, status = 'active') {
    const step = document.getElementById(`step-${stepName}-${sectionId}`);
    if (step) {
      step.className = `step ${status}`;
    }
  }

  function showAddressStatus(message, type) {
    if (statusContainer) {
      statusContainer.textContent = message;
      statusContainer.className = `address-status ${type}`;
    }
  }

  // ===== AUTOCOMPLÉTION ADRESSE ===== 
  
  async function searchAddress(query) {
    if (query.length < 3) {
      if (suggestionsContainer) {
        suggestionsContainer.style.display = 'none';
      }
      return;
    }

    try {
      const response = await fetch(`https://api-adresse.data.gouv.fr/search/?q=${encodeURIComponent(query)}&limit=5`);
      const data = await response.json();
      displaySuggestions(data.features || []);
    } catch (error) {
      console.error('Erreur API Adresse:', error);
      showAddressStatus('Erreur lors de la recherche d\'adresse', 'error');
    }
  }

  function displaySuggestions(features) {
    if (!suggestionsContainer) return;
    
    suggestionsContainer.innerHTML = '';
    
    if (features.length === 0) {
      suggestionsContainer.style.display = 'none';
      return;
    }

    features.forEach(feature => {
      const suggestion = document.createElement('div');
      suggestion.className = 'address-suggestion';
      suggestion.textContent = feature.properties.label;
      
      suggestion.addEventListener('click', function() {
        selectAddress(feature);
      });
      
      suggestionsContainer.appendChild(suggestion);
    });
    
    suggestionsContainer.style.display = 'block';
  }

  function selectAddress(feature) {
    selectedAddress = feature;
    if (addressInput) {
      addressInput.value = feature.properties.label;
    }
    if (suggestionsContainer) {
      suggestionsContainer.style.display = 'none';
    }
    showAddressError(false);
    
    const codeCommune = feature.properties.citycode;
    showAddressStatus(`✅ Adresse validée - Code commune: ${codeCommune}`, 'success');
  }

  // ===== AFFICHAGE RÉSULTATS v5.2 =====
  
  function displayLifeWaterResults(scoreResult, address, parametersData) {
    console.log('=== AFFICHAGE RÉSULTATS LIFE WATER v5.2 ===');
    console.log('Score:', scoreResult.score, 'Fiabilité:', scoreResult.fiabilite + '%');
    console.log('Paramètres testés:', scoreResult.metadata.parametres_testes_total + '/' + scoreResult.metadata.parametres_totaux_total);
    
    if (!analysisResults) return;
    
    if (analysisLoading) {
      analysisLoading.style.display = 'none';
    }
    
    const htmlLifeWater = generateLifeWaterHTML(
      scoreResult, 
      address.properties.label, 
      parametersData
    );
    
    analysisResults.innerHTML = htmlLifeWater;
    analysisResults.style.display = 'block';
    
    // Animation du score avec fiabilité
    setTimeout(() => {
      const scoreNumber = analysisResults.querySelector('.score-number');
      const fiabilityFill = analysisResults.querySelector('.fiabilite-fill');
      const contributionFills = analysisResults.querySelectorAll('.contribution-fill');
      
      // Animation score principal
      if (scoreNumber && !isNaN(scoreResult.score)) {
        let currentScore = 0;
        const targetScore = scoreResult.score;
        const duration = 1500;
        const increment = targetScore / (duration / 16);
        
        const animateScore = () => {
          currentScore += increment;
          if (currentScore >= targetScore) {
            scoreNumber.textContent = targetScore;
          } else {
            scoreNumber.textContent = Math.floor(currentScore);
            requestAnimationFrame(animateScore);
          }
        };
        
        animateScore();
      }
      
      // Animation barre de fiabilité
      if (fiabilityFill) {
        setTimeout(() => {
          fiabilityFill.style.width = scoreResult.fiabilite + '%';
        }, 500);
      }
      
      // Animation barres de contribution
      if (contributionFills) {
        contributionFills.forEach((fill, index) => {
          setTimeout(() => {
            const scoreValue = scoreResult.contributions[Object.keys(scoreResult.contributions)[index]].score;
            fill.style.width = scoreValue + '%';
          }, 700 + index * 100);
        });
      }
      
    }, 100);
    
    analysisResults.scrollIntoView({ behavior: 'smooth', block: 'center' });
    
    console.log('✅ AFFICHAGE LIFE WATER v5.2 TERMINÉ');
  }

  // ===== ÉVÉNEMENTS =====
  
  // Autocomplétion d'adresse
  if (addressInput) {
    addressInput.addEventListener('input', function() {
      const query = this.value.trim();
      
      if (selectedAddress && this.value !== selectedAddress.properties.label) {
        selectedAddress = null;
        if (statusContainer) {
          statusContainer.style.display = 'none';
        }
      }
      
      clearTimeout(debounceTimer);
      debounceTimer = setTimeout(() => {
        searchAddress(query);
      }, 300);
    });

    document.addEventListener('click', function(e) {
      if (addressInput && suggestionsContainer && 
          !addressInput.contains(e.target) && !suggestionsContainer.contains(e.target)) {
        suggestionsContainer.style.display = 'none';
      }
    });
  }

  // Bouton d'analyse avec algorithme scientifique v5.2
  if (startWaterAnalysisBtn) {
    startWaterAnalysisBtn.addEventListener('click', async function(e) {
      e.preventDefault();
      
      console.log('=== DÉBUT ANALYSE EAU v5.2 - ALGORITHME ÉQUITABLE ===');
      
      if (!selectedAddress) {
        showAddressError(true);
        if (addressInput) {
          addressInput.focus();
        }
        return;
      }
      
      const analyseApprofondie = document.getElementById(`analysis-detailed-${sectionId}`)?.checked || false;
      
      if (analysisForm) {
        analysisForm.style.display = 'none';
      }
      if (analysisLoading) {
        analysisLoading.style.display = 'block';
      }
      
      try {
        // ÉTAPE 1 : Géocodage
        updateProgressStep('geocoding', 'active');
        if (loadingText) {
          loadingText.textContent = 'Validation de l\'adresse...';
        }
        
        await new Promise(resolve => setTimeout(resolve, 1000));
        updateProgressStep('geocoding', 'completed');
        
        // ÉTAPE 2 : Récupération données avec fallback
        updateProgressStep('data', 'active');
        if (loadingText) {
          loadingText.textContent = 'Recherche des données de qualité d\'eau...';
        }
        
        const codeCommune = selectedAddress.properties.citycode;
        const lat = selectedAddress.geometry.coordinates[1]; // latitude
        const lon = selectedAddress.geometry.coordinates[0]; // longitude
        
        // Recherche avec fallback géographique
        const hubeauResult = await fetchHubeauDataWithFallback(codeCommune, lat, lon, 20);
        
        // Mise à jour du message selon le type de source
        if (hubeauResult.sourceInfo.type === 'commune_voisine') {
          if (loadingText) {
            loadingText.textContent = `Données trouvées à ${hubeauResult.sourceInfo.nomCommune} (${hubeauResult.sourceInfo.distance.toFixed(1)}km)...`;
          }
        } else if (hubeauResult.sourceInfo.type === 'aucune_donnee') {
          if (loadingText) {
            loadingText.textContent = 'Recherche étendue effectuée, données limitées...';
          }
        }
        
        await new Promise(resolve => setTimeout(resolve, 1500));
        updateProgressStep('data', 'completed');
        
        // ÉTAPE 3 : Analyse équitable
        updateProgressStep('analysis', 'active');
        if (loadingText) {
          loadingText.textContent = 'Analyse de TOUS les paramètres importants...';
        }
        
        await new Promise(resolve => setTimeout(resolve, 800));
        
        // ÉTAPE 4 : Scoring scientifique équitable v5.2
        updateProgressStep('scoring', 'active');
        if (loadingText) {
          loadingText.textContent = 'Calcul équitable avec bénéfice du doute...';
        }
        
        // 🔥 NOUVEAU: Algorithme scientifique équitable v5.2
        const scoreResult = calculateLifeWaterScore(
          hubeauResult.parametersData, 
          { analyseApprofondie }, 
          hubeauResult.sourceInfo
        );
        
        await new Promise(resolve => setTimeout(resolve, 1000));
        updateProgressStep('analysis', 'completed');
        updateProgressStep('scoring', 'completed');
        
        // Affichage résultats
        displayLifeWaterResults(scoreResult, selectedAddress, hubeauResult.parametersData);
        
      } catch (error) {
        console.error('Erreur lors de l\'analyse:', error);
        
        const activeStep = document.querySelector(`#analysis-loading-${sectionId} .step.active`);
        if (activeStep) {
          activeStep.className = 'step error';
        }
        
        if (loadingText) {
          loadingText.textContent = 'Erreur lors de l\'analyse. Veuillez réessayer.';
        }
        
        setTimeout(() => {
          if (analysisLoading) {
            analysisLoading.style.display = 'none';
          }
          if (analysisForm) {
            analysisForm.style.display = 'block';
          }
        }, 3000);
      }
    });
  }

});
</script>

{% schema %}
{
  "name": "Water Analyzer v5.2",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "default": "Analyseur Scientifique Équitable de Qualité de l'Eau",
      "label": "Titre de la section"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description",
      "default": "<p>Découvrez la qualité de votre eau potable grâce à notre algorithme scientifique v5.2 équitable avec transparence totale sur tous les paramètres importants.</p>"
    }
  ],
  "presets": [
    {
      "name": "Analyseur Scientifique Équitable de Qualité de l'Eau v5.2"
    }
  ]
}
{% endschema %}
